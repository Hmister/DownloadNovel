<Window x:Class="DownloadNovel.ReadOnline"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DownloadNovel"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        mc:Ignorable="d"
        Title="ReadOnline" Height="717.333" Width="580" Loaded="Window_Loaded">
    <StackPanel Margin="10">

        <StackPanel Margin="10,0,10,0">
            <hc:Divider Name="zj" Content="章节" Padding="15,0" Margin="10" FontSize="14"/>
        </StackPanel>

        <WrapPanel Margin="0">
            <FlowDocumentScrollViewer IsToolBarVisible="True" Margin="10,0,10,10" Height="573">
                <FlowDocument ColumnWidth="400" IsOptimalParagraphEnabled="True" IsHyphenationEnabled="True">
                    <Section FontSize="14">
                        <Paragraph x:Name="nr">
                            <Run Text="等待加载。。。"/>
                        </Paragraph>
                        <Paragraph>
                            <Run Text="Tips：请使用键盘左右键（← →)进行翻页~"/>
                        </Paragraph>
                    </Section>
                </FlowDocument>
            </FlowDocumentScrollViewer>
        </WrapPanel>

        
    </StackPanel>
</Window>
